# any files named the same thing but are either .py or .ipynb contain the same code just in different formats
# 'completed_df.csv' is the dataset that contains pass plays and the pocket time of the quarterbacks
